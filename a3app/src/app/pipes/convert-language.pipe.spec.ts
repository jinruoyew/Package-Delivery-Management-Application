import { ConvertLanguagePipe } from './convert-language.pipe';

describe('ConvertLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
